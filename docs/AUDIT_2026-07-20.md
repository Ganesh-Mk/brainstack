# 🔍 Full-platform audit — 2026-07-20

> Run after all 10 product phases shipped. Method: every test suite, every
> phase's e2e against production, every route, code-level security and
> dead-code sweeps, docs accuracy. Findings fixed, then re-verified.

## Round 1 — findings & fixes

| # | Finding | Severity | Fix |
|---|---|---|---|
| 1 | Phase 6–8 e2e cleanup blocks broke with FK violations — Phases 8/9 added `memories`/`query_traces` referencing users/tenants, older scripts never deleted them | med (prod data hygiene) | cleanup order unified across all scripts (query_traces → memories → … → tenants) |
| 2 | 5 orphaned e2e tenants in the prod DB from the failed cleanups + the reranker-OOM interrupted run | med | swept (only the real tenant remains); 4 orphaned Pinecone namespaces deleted too (incl. the archived `_lab`) |
| 3 | Self-inflicted during audit: the orphan sweep ran while the e2e battery was mid-flight and deleted a live test tenant | process | noted in memory: never sweep during a battery |
| 4 | Dead components (`QuestionsAskedTile`, `DocsIndexedTile`) and the retired Phase-5 `chat.stream_answer` path | low | removed |
| 5 | Stale phase-language in Settings→Account toast and a Topbar comment ("arrives with Backend Phase 0" — it shipped) | low | reworded honestly |
| 6 | PHASE_9_COMPLETE claimed "70 → 79 tests" (double-counted its own additions) | low (docs) | corrected |
| 7 | Anaphoric follow-ups ("which of THOSE error codes…") intermittently produced a search query that missed — 2 of 3 prod runs declined, while the standalone phrasing retrieved perfectly (probe-verified) | med (answer quality) | routing rule added: rebuild a SELF-CONTAINED query from the conversation (the guide's rule — fix the prompt, not the loop); re-verified twice in prod |

## Round 2 — verification

- Backend **76/76**, company **9/9**, frontend build clean (41 routes).
- Production e2e battery, all five suites, sequentially:
  Phase 6 **16/16** · Phase 7 **20/20** · Phase 8 **12/13** (finding #7,
  fixed and re-run — see round 3) · Phase 9 **12/12** · Phase 10 **13/13**.
- All 28 app+marketing routes return 200 (auth redirects 307 by design).
- Auth sweep: only `/health` is public; every other router derives
  tenant/role from `get_current_user()` — the cardinal rule held everywhere
  it was checked, including cross-tenant 404s on documents, conversations,
  memories and members.
- Remaining intentionally-unbuilt surfaces: Settings → Billing and
  API keys ("Later · after launch"); profile name/email editing.

## Round 3 — clean

After the finding-#7 routing fix deployed, Phase 8 e2e ran **13/13 twice
consecutively** in production (the follow-up now searches with a
self-contained query and answers correctly both times). All suites and all
five production e2e batteries green. **Audit closed.**

## Observed in the wild during the audit

- A **reflection rewrite fired on a real production question** (web-search
  answer judged ungrounded → reset → rewrite → passed all checks) — the
  Phase 8 loop working unprompted, capped, and visible in the trace.
- The observability tables caught a 26.9s cold-start ask with 26.2s
  first-token latency — free-tier napping, now measurable.
