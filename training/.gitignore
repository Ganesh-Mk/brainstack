# The exported model itself — ~2GB, rebuilt by finetune_colab.ipynb from the
# LoRA adapter. The pipeline that produces it is what belongs in git.
*.gguf

# Modelfile is GENERATED by make_modelfile.py from chat.SYSTEM_PROMPT. Kept
# out so it can never drift from the prompt it is supposed to mirror — run
# `python training/make_modelfile.py` to produce it.
Modelfile
